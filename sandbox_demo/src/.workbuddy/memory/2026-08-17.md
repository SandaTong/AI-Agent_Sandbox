# 2026-08-17 工作日志

## sandbox_demo — M7 完成 + M0→M7 知识盘点
- M7(DNS 域名维度网络管控)已完成,网络管控从 IP 维度升到域名维度。
- **没有新增 core 文件**:M7 复用 M4 injector + M6 wfp_filter + M0~M2 Job/Token/Mitigation。
- 新增 `demo/dns_hook.cc` → `dns_hook.dll`:域名管控垫片 DLL。hook `ws2_32!GetAddrInfoW`(域名解析主入口)。
  - 白名单通过环境变量 `M7_DNS_ALLOWLIST` 传递(分号分隔,支持 `*.` 通配)。命中放行,未命中返回 WSAHOST_NOT_FOUND。
  - 复用 M4 注入+日志双通道+MinHook 装钩做法,只换 hook 目标。
- 新增 `demo/m7_demo.cc`(形态 A:DNS 域名白名单 Hook):挂起→注入 dns_hook.dll→resume。
- 新增 `demo/m7_ip_demo.cc`(形态 C:DNS→IP 联动 M6 WFP):broker 侧解析白名单域名→IP,联动 WFP 做纵深防御。继承 M6 IP 不命中限制,用 AppID 兜底。
- `hello_target.cc` 新增 `Test8_ResolveDomain`(jailbreak-8a example.com 放行 / 8b www.bing.com 拦截)。
- 核心洞察:为什么 hook 而非 WFP 拦 :53——DNS 解析走进程外 dnscache 服务,WFP 在 :53 看到的源是 svchost 不是 target,区分不了进程;进程内 hook 才能拿明文域名+区分进程+挡 DoH。
- 已更新 MEMORY.md:M7 段落加入路线图,头部更新为"M0~M7 全部完成"。
- 做了 M0→M7 全部机制的知识盘点(共 29 个独立机制),用全景图对照。
