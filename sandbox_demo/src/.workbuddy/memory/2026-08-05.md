# 2026-08-05

## 工作内容
- 阅读 `core/token_manager.cc`,讲解了 `OpenProcessToken` 在创建受限令牌流程中的作用
  (取当前进程 token 句柄,申请 TOKEN_DUPLICATE|QUERY|ASSIGN_PRIMARY|ADJUST_DEFAULT 最小权限集,
  为后续 `CreateRestrictedToken` + `CreateProcessAsUserW` 做准备)。
- 用可视化方式系统讲解了 Windows 访问令牌:内部结构(SID/组/特权/IL/DACL/Logon SID)、
  访问检查流程(SRM 比对 DACL 的 ACE)、生命周期(登录生成→进程继承→线程使用/impersonation),
  并与 sandbox_demo 的"裁剪 token 限制子进程"思路对应。
- 通读 `token_manager / job_manager / process_launcher / m0_demo` 四个文件,修正用户对项目形态的理解:
  M0 不是只靠 token 裁剪,而是"受限 token + Job Object"双支柱(token 管"特权能力",Job 管"资源/生命周期"),
  经 CREATE_SUSPENDED → AssignToJob → Resume 三步舞启动子进程,保证 Job 约束在第一行代码前生效。
  确认了 M1/M2/M3 路线图(IL 降级、LowBox Token/AppContainer、Broker-Target IPC)。

## 记忆
- 已将项目领域知识(沙箱原理、M0 双支柱架构、三步舞、路线图、技术约定)写入 MEMORY.md。
