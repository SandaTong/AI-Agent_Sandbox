# 2026-08-12 工作日志

## sandbox_demo — M5 完成记录
- M5(target 侧运行时自检 / 反注入反篡改检测层)已完成,这是 M4 的攻防翻转版。
- 新增 `core/self_defense.{h,cc}`:`SelfDefense` 类,跑在 target 内部(与 M0~M4 broker 外部配置的根本区别)。
  - 三个检测手段对应 M4 三种攻击向量:
    ① DLL 加载通知(`LdrRegisterDllNotification`)——抓 M4 远程线程 LoadLibraryW 注入
    ② 可疑远程线程扫描(`NtQueryInformationThread(ThreadQuerySetWin32StartAddress=9)`)——抓 CreateRemoteThread
    ③ 关键 API inline-hook 篡改自检(读 kernel32!CreateFileW 头字节)——抓 MinHook 改函数头
  - `DefenseFinding` 三种 Kind:kSuspiciousDll / kSuspiciousThread / kApiTampered。
- 新增 `demo/m5_demo.cc`:三种对照模式(纯防御 / 防御+攻击 / 关防御+攻击)。
  M5 mitigation 默认开满反注入三件套(prohibit_dynamic_code + strict_signed_dll + disable_extension_points),`--defense-off` 关闭复现 M4。
- `demo/hello_target.cc` 接入 M5:新增 `--selfcheck` 参数,启动时尽早装 DLL 加载监控。
- 新增 `demo/unsigned_probe.cc`:非微软签名探测 DLL(M1 BLOCK_NON_MICROSOFT_BINARIES 验证用)。
- 核心定位:"内核挡(mitigation)+ 用户态查(SelfDefense)"双层防御,M5 是第二道,补内核 mitigation 两个盲区(只拦不报 / 开不满时兜底)。
- 已更新 MEMORY.md:M5 段落加入路线图,项目目标头部更新为"M0~M5 全部完成"。
