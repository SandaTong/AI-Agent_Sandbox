# 2026-08-14 工作日志

## sandbox_demo — M6 完成 + M0→M6 知识盘点
- M6(WFP 用户态网络管控)已完成,还 M2 留下的两笔网络债(loopback bypass + 只能按 Package SID 匹配)。
- 新增 `core/wfp_filter.{h,cc}`:`WfpFilter` 类,用户态 WFP 过滤器管理器。
  - 四概念:Engine(FwpmEngineOpen0, DYNAMIC 会话) / Layer(ALE_AUTH_CONNECT_V4) / Sublayer(自建归组) / Filter(condition+action+weight)。
  - 两种形态:kIpBlocklist(IP 黑名单,BLOCK+scope AppID) / kBlockAppId(按 exe 全拦 outbound)。
  - DYNAMIC 会话:句柄一关 BFE 自动清理所有 filter,进程崩也不残留脏规则。
- 新增 `demo/m6_demo.cc`(形态 A,IP 黑名单) + `demo/m6_appid_demo.cc`(形态 C,AppID 精确拦截,含 loopback)。
- M6 踩坑:IP 精确匹配(FWP_V4_ADDR_MASK)在本机 ALE_AUTH_CONNECT_V4 层不命中(四种组合全装配正确却拦不住),AppID 条件正常生效 → 精确管控用 kBlockAppId。
- 已更新 MEMORY.md:M6 段落加入路线图,头部更新为"M0~M6 全部完成"。
- 做了 M0→M6 全部机制的知识盘点(共 24 个独立机制),用全景图对照。
